<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".MainActivity">

    <item
        android:id="@+id/action_bluetooth_connect"
        android:title="@string/action_bluetooth_connect"
        android:icon="@drawable/ic_action_device_bluetooth"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_bluetooth_disconnect"
        android:title="@string/action_bluetooth_disconnect"
        android:icon="@drawable/ic_action_device_bluetooth_disabled"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_shop"
        android:title="@string/action_shop"
        android:icon="@drawable/ic_action_shopping_basket"
        app:showAsAction="ifRoom"/>

</menu>
